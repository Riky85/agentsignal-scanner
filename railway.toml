[build]
builder = "NIXPACKS"
buildCommand = "pip install aiohttp beautifulsoup4 lxml asyncpg"

[deploy]
startCommand = "python industrial_runner.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
