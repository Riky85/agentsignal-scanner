[build]
builder = "nixpacks"
buildCommand = "pip install requests"

[deploy]
startCommand = "python3 feeder_runner.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "always"
restartPolicyMaxRetries = 5
