[build]
builder = "NIXPACKS"
buildCommand = "pip install aiohttp beautifulsoup4 lxml"

[deploy]
startCommand = "python3 industrial_crawler.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
